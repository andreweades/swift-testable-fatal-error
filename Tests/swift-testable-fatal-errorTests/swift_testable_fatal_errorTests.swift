import XCTest
@testable import swift_testable_fatal_error

final class swift_testable_fatal_errorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_testable_fatal_error().text, "Hello, World!")
    }
}
